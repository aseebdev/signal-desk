{
  "name": "signaldesk",
  "version": "1.1.0",
  "private": true,
  "description": "Local-first personal action intelligence workspace.",
  "scripts": {
    "start": "node dev-server.cjs",
    "check": "node --check dev-server.cjs && node --check script.js && node --check api/users.js && node --check api/health.js"
  },
  "engines": {
    "node": "24.x"
  }
}
